# Infrastructure

Documentação do `BeeDay.Infrastructure` fora da persistência: caching, background jobs, health
checks, e a implementação técnica de Identity. Para EF Core/SQL Server especificamente, ver
[`docs/persistence/`](../persistence/README.md).

**Fonte da verdade:** ainda não há documento próprio nesta pasta — quando criado, deverá ser
verificado diretamente contra `src/BeeDay.Infrastructure/Caching`, `Background`, `HealthChecks` e
`Identity`.

## Documentos

Nenhum documento vive aqui ainda.

## Ordem de leitura recomendada

Não aplicável ainda — pasta reservada pela taxonomia da Sprint 16.2, conteúdo a ser escrito em
Sprint futura (ver relatório da Sprint 16.2, seção "Documentos adiados").
