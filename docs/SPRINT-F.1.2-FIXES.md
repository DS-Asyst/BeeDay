# Sprint F.1.2 — Form binding and card dragging fixes

## Corrections

- Fixed string form controls that rendered the literal text `Value` instead of the bound editor model value.
- Title and notes fields now start empty when creating Habits, Tasks, To-Dos, and Projects.
- Sortable card wrappers are now natively draggable from initial render.
- Dragging starts from the card body while interactive controls remain excluded.
- Existing touch long-press and keyboard arrow reordering are preserved.
