# Persistence

The current persistence provider is JSON.

Infrastructure is isolated so the storage implementation can be replaced by SQL Server or another provider in the future without changing Domain or Application.