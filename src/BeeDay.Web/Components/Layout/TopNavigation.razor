<header class="top-navigation">
    <div class="top-navigation__content">
        <button type="button"
                class="top-navigation__brand-button"
                aria-label="@(IsProfilePanelOpen ? "Close profile panel" : "Open profile panel")"
                aria-expanded="@IsProfilePanelOpen"
                @onclick="OnToggleProfilePanel">
            <span class="top-navigation__brand-text" aria-hidden="true">
                <span class="top-navigation__brand-level">BEE</span>
                <span class="top-navigation__brand-up">DAY</span>
            </span>

            <span class="top-navigation__panel-arrow" aria-hidden="true">
                <PixelIcon Name="@(IsProfilePanelOpen ? PixelIconName.ChevronLeft : PixelIconName.ChevronRight)" Size="PixelIconSize.Medium" />
            </span>
        </button>

        <nav class="top-navigation__links" aria-label="Main navigation">
            <NavLink href="/daily" Match="NavLinkMatch.Prefix">Daily</NavLink>
            <NavLink href="/wallet" Match="NavLinkMatch.Prefix">Wallet</NavLink>
        </nav>


        <div class="top-navigation__actions @(IsMenuPanelOpen ? "is-menu-open" : null)">
            <button type="button"
                    class="top-navigation__menu-button"
                    aria-label="@(IsMenuPanelOpen ? "Close support menu" : "Open support menu")"
                    aria-expanded="@IsMenuPanelOpen"
                    @onclick="OnToggleMenuPanel">
                <span class="top-navigation__menu-icon" aria-hidden="true">
                    <PixelIcon Name="@(IsMenuPanelOpen ? PixelIconName.Close : PixelIconName.Menu)" Size="PixelIconSize.Large" />
                </span>
            </button>
        </div>
    </div>
</header>

@code {
    [Parameter] public bool IsProfilePanelOpen { get; set; }
    [Parameter] public bool IsMenuPanelOpen { get; set; }
    [Parameter] public EventCallback OnToggleProfilePanel { get; set; }
    [Parameter] public EventCallback OnToggleMenuPanel { get; set; }
}
