<button type="@Type"
        class="@CssClasses"
        disabled="@IsDisabled"
        aria-busy="@IsLoading.ToString().ToLowerInvariant()"
        @onclick="HandleClickAsync"
        @attributes="AdditionalAttributes">
    @if (IsLoading)
    {
        <PixelIcon Name="PixelIconName.Loading" Size="@IconSize" Class="levelup-button__loader" />
    }
    else if (Icon is not null)
    {
        <PixelIcon Name="@Icon.Value" Size="@IconSize" />
    }
    <span class="levelup-button__label">@ChildContent</span>
</button>
