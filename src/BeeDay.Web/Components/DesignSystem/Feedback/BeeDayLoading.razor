@if (IsVisible)
{
    <div class="beeday-loading-overlay" role="status" aria-live="polite" aria-label="@Label">
        <PixelIcon Name="PixelIconName.Loading" Size="PixelIconSize.Small" Color="PixelIconColor.Primary" Class="beeday-loading-spinner" />
        <span class="beeday-loading-label">@Label</span>
    </div>
}

@code {
    [Parameter] public bool IsVisible { get; set; }
    [Parameter] public string Label { get; set; } = "Saving changes...";
}
