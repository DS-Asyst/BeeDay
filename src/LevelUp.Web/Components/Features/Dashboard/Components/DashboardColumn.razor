<section class="dashboard-column" aria-labelledby="@HeadingId">
    <header class="dashboard-column__header">
        <div class="dashboard-column__heading-group">
            <div class="dashboard-column__heading">
                <h2 id="@HeadingId">@Title</h2>
                <span class="dashboard-column__count" aria-label="@CurrentCountLabel">@CurrentCount</span>
            </div>

            @if (HeaderContent is not null)
            {
                <div class="dashboard-column__header-content">
                    @HeaderContent
                </div>
            }
        </div>

        <div class="dashboard-column__actions">
            @if (ShowCompletedSection)
            {
                <button class="dashboard-column__view-toggle"
                        type="button"
                        aria-pressed="@AriaPressed"
                        aria-label="@ToggleViewLabel"
                        title="@ToggleViewLabel"
                        @onclick="ToggleCompleted">
                    <PixelIcon Name="@CurrentViewIcon" Size="PixelIconSize.Small" Decorative="true" />
                </button>
            }

            @if (ShowCreateButton)
            {
                <button class="dashboard-column__add"
                        type="button"
                        aria-label="@($"Add {ResolvedSingularLabel}")"
                        title="@($"Add {ResolvedSingularLabel}")"
                        @onclick="OnCreate">
                    <PixelIcon Name="PixelIconName.Add" Size="PixelIconSize.Small" Decorative="true" />
                </button>
            }
        </div>
    </header>

    <div class="dashboard-column__viewport">
        <div class="dashboard-column__content">
            @if (showCompleted && ShowCompletedSection)
            {
                @if (CompletedCount == 0)
                {
                    <LevelUpEmptyState Class="dashboard-column__empty"
                                       Icon="PixelIconName.Completed"
                                       Title="@CompletedEmptyTitle"
                                       Description="@CompletedEmptyDescription" />
                }
                else
                {
                    @CompletedContent
                }
            }
            else if (ActiveCount == 0)
            {
                <LevelUpEmptyState Class="dashboard-column__empty"
                                   Icon="@EmptyIcon"
                                   Title="@EmptyTitle"
                                   Description="@EmptyDescription" />
            }
            else
            {
                @ActiveContent
            }
        </div>
    </div>
</section>
