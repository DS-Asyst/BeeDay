@if (Attribute is not null)
{
    <PixelIcon Name="@IconName"
               Size="@Size"
               Color="@Color"
               Decorative="@Decorative"
               Label="@(!Decorative ? Label : null)"
               Class="@CssClass" />
}
