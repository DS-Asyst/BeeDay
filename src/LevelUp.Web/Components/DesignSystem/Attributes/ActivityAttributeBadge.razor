@if (Attribute is not null)
{
    <span class="activity-attribute-badge activity-attribute-badge--@AttributeCssName" data-attribute="@Attribute" title="@Tooltip" aria-label="@Tooltip">
        <span>@Attribute</span>
    </span>
}
