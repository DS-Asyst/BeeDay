@typeparam TValue
@using System.Linq.Expressions

<label class="@FieldCssClass" for="@Id">
    @if (!string.IsNullOrWhiteSpace(Label))
    {
        <span class="@LabelCssClass">
            @Label
            @if (Required)
            {
                <span aria-hidden="true">*</span>
            }
        </span>
    }
    <span class="levelup-field__control-shell">
        <InputDate TValue="TValue"
                   id="@Id"
                   class="@InputCssClass"
                   Value="Value"
                   ValueChanged="ValueChanged"
                   ValueExpression="ValueExpression"
                   disabled="@Disabled"
                   @attributes="AdditionalAttributes" />
        <PixelIcon Name="PixelIconName.Calendar" Size="PixelIconSize.Small" Color="PixelIconColor.Muted" Class="levelup-field__control-icon" />
    </span>
</label>
@if (ShowValidationMessage)
{
    <LevelUpValidationMessage For="ValueExpression" />
}
