@if (Segments.Count == 0)
{
    @Text
}
else
{
    @foreach (var segment in Segments)
    {
        if (segment.IsMatch)
        {
            <mark class="levelup-search-highlight">@segment.Value</mark>
        }
        else
        {
            @segment.Value
        }
    }
}
